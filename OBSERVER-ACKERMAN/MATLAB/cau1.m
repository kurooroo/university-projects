clear all;
clc;
A=[0 1 0;0 0 1;-15 -23 -9];
B=[0;0;1];
C=[-13.5 -18.25 -5];
D=1;
syms s t;
Gs=C*inv(s*eye(3)-A)*B+D;
eAt=ilaplace(inv(s*eye(3)-A));
minus_eAt=(eAt*-1)*B;
int_eAt=int(minus_eAt,t,0);
xt=int_eAt;
yt=C*xt;
AT=A';
CT=C';
sT = [0 0 1]*inv([CT, AT*CT, (AT^2)*CT]);
S=[sT;sT*AT;sT*AT*AT];
%L=acker(A',C',[-1.8+2.4i;-1.8-2.4i;-4.5]);
L=(81/2)*sT+(126/5)*sT*AT+(81/10)*sT*AT*AT+sT*AT*AT*AT;
L=L';
Aobs=[A-L*C];
Bobs=[B-L*D L];
Cobs=eye(3);
Dobs=zeros(3,2);